.dropdown-
  &divider
    c 0 $border
  &signed-commit &menu-w
    caret "left" $bg
  &menu
    c $text $border $alt
    &-ne
      caret "top" $hl
    &:after
      bbc $border
  &item
    c $text
    ease background-color, color
    &:hover, &:focus
      c $cn 0 $region
