.dropdown-
  &divider
    c 0 $border
  &signed-commit &menu-w
    caret "left" $bg
  &menu
    c $text $border $alt
    &:after
      border-bottom-color $border
  &item
    c $text
    tr background-color, color 0.1s ease-in-out
    &:hover, &:focus
      c $cn 0 $region
