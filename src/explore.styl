.news
  .alert
    bt $b
  .Details
    &, &-content--hidden li, button
      btc base(1)
    button:hover
      bg $t
      td underline
div[style*="linear-gradient"]
  fade-out()
.exploregrid-item, .height-full.border.border-gray-light.rounded-1.p-3.p-md-5.my-5
  c 0 0 base(5)
.newsletter-frequency-choice
  c 0 base(1)
  r 8px
  h3
    c 0 base(1) base(5)
