.UnderlineNav
  c 0 1 3
  ease colors
  /nav& + div.border.d-md-block
    c 0 0 4
  &[style*="margin-top"]
    c 0 $t 2
  &-item
    ease colors
    ^[0]-octicon
      c fg("faded")
    +selected()
      bbc v("blue")
      octicon fg("light")
    &:hover, &:focus
      bbc base(8)
      octicon fg("primary")
