@require "common"
@require "notifications"
@require "syntax"
@require "calendar"
@require "markdown"
@require "projects"
@require "plugins"
@require "color-replacements"
@require "pull-requests"
@require "actions"
@require "insights"
@require "explore"
@require "search"
@require "release"
@require "billing"
@require "marketplace"
@require "dropdowns"
@require "issues"
@require "commits"
@require "dashboard"
@require "team"

*
  sbc $sb $bg
::selection
  c $cn 0 $region
::placeholder
  c $placeholder
body
  c $text 0 $bg
  fn $uiFont, sans-serif
  fs 14px
  lh 1.8
  &.page-responsive.pagehead
    c 0 0 $bg
input
  caret-color $blue
img
  c 0 0 $alt
h1
  c $text
mark
  c $yellow
kbd
  c $text $border $bg
  font 14px $monoFont
  s inset 0 -1px 0 $border
ol, ul
  list-style disc
legend
  fs 0.9em
  fw 600
input,
select,
button,
textara
  &:disabled
    cursor not-allowed
    o 0.5
fieldset
  b $b
  m 0
  mb 6px
  p 10px
  r 6px
code, pre, tt
  mono()
.form-select
  bgi url("data:image/svg+xml,%3Csvg width='24' height='24' viewBox='0 0 24 24' fill='none' stroke='%23c8d3f5' stroke-width='2' stroke-linecap='round' stroke-linejoin='round' class='feather feather-chevron-down' %3E%3Cpolyline points='6 9 12 15 18 9' /%3E%3C/svg%3E")
.blob-
  &code-inner
    c $text
    fs 15px
    lh 1.8
    mono()
    .highlighted
      c 0 0 $icon
  &num
    c $hover $border $alt
    fw 600
  &expanded
    &code, &num
      c 0 $border $alt
.file:target
  s-1()
.description
  c $text
.State--
  &green
    b 1px solid $teal
    c $teal $teal $tealTrans
    .octicon
      v $teal
  &purple
    b 1px solid $purple
    c $purple $purple $purpleTrans
    .octicon
      v $purple
  &red
    b 1px solid $red
    c $red $red $redTrans
    .octicon
      v $red
img[src*="octocat-spinner-64.gif"]
  inv()
.form
  &-control, &-select
    &.focus, &:focus
      c $cn $blue 0
      s none
.octicon
  color $text
  &-repo
    &, &-push, &-forked
      mb 0.1em
  &.merged, &-git-pull-request
    c $purple
  &-repo
    c $faded
  &-play
    c $redLight
  &-x, &-trashcan
    c $red
  &-repo-push, &-check, &.open
    c $green
  &[style*="#6a737d"]
    c $textGray
  &-alert, &-pencil, &-star, &-light-bulb
    c $yellow
  &-repo-forked, &-issue-opened
    c $teal
  &-issue-closed
    c $red
  &-project, &-eye
    c $blue
  &-package, &-rss, &-flame
    c $orange
  &-smiley, &-file
    c $indigo
.muted-link
  c $faded
  &:hover
    c $blue
.js-recent-activity-container > div > div
  c 0 0 $bg
.diffstat-
  &block-
    &neutral
      c 0 0 $sb
    &added
      c 0 0 $green
    &deleted
      c 0 0 $red
  &summary
    c $text
    strong
      c $purple
path[fill="#eee"]
  v $icon
.file-diff-split .empty-cell
  c 0 $border $alt
.blob-code-
  &addition .x
    c 0 0 $teal
  &deletion .x
    c 0 0 $red
  &addition, &deletion
    .x
      c $border
.Label--
  &gray
    c $text $border $bg
  &outline
    c $text $border
.repository-lang-stats ol.repository-lang-stats-numbers li .lang
  c $cn
comment-form
  .previewable-& .&-head.tabnav
    bb $b
    bl 0
    br 0
    bt 0
    c 0 0 $alt
.tabnav
  bb $b
  /.container-preview &-tabs
    ml 5%
  &-tab
    c $faded
  &-tabs.js-file-editor-nav &-tab
    c $faded $border $bg
    mt 8px
    rt 6px
  &--responsive &-tab, &-tab
    c $faded $border
    &[aria-current], &[aria-selected=true], &.selected
      c $text $border $hl
  .&-pr, div
    ../-tab
      &.selected
        &.js-blob-edit-
          &code, &preview
            c $blue $border $hl
        c $blue $border $bg
      .Counter
        c $blue
.branch-name
  c $text $border $region
.select-menu-
  &item .octicon-check
    c $green
  &header, &header &title
    c $cn $border $bg
  &modal, &item
    c $text $border $alt
  &divider
    c $text $border $alt
  &filters
    c $text $border $hl
  &text-filter
    &:first-child, &:last-child
      c 0 $border
  &modal
    s-2()
.pagination
  .current, [aria-current]
    c $cn 0 $icon
  a, em, span, .disabled, .gap, [aria-disabled=true]
    &:hover
      c 0 0 $region
    ease background-color
    c 0 $border $hl
input[type="text"]
  c $cn $border $alt
div[style*="linear-gradient"]
  bg linear-gradient(to top, rgba(0, 0, 0, 1), rgba(0, 0, 0, 0))
.numbers-summary
  octicon $text
-expander
  .ellipsis&, .hidden-text& a
    c $text 0 $icon
.UnderlineNav
  c 0 $border $hl
  ease background-color, border-color
  &[style*="margin-top"]
    c 0 transparent $bg
  &-item
    /.subnav-link.selected, &.selected, &[aria-current], &[aria-selected=true]
      bbc $blue
.SelectMenu-
  &item
    c $text $border
  &icon--check
    c $green
  &divider
    c $text $border $border
  &list
    c 0 $border $bg
  &item, &modal
    c 0 $border $alt
  &header
    bbc $border
  &footer
    c $text $border $alt
    kbd
      fs unset
  &filter
    btc $border
  &tab
    c $text $border $bg
    &[aria-selected=true]
      c $cn $border $alt
.TimelineItem
  &--condensed &-badge
    size 30px
  &-break
    c 0 $alt transparent
.TableObject-item--primary
  c $text
.btn-mktg
  c $cn $border $bg
.Header
  c $text $border $bg
.gh-header
  background-color unset
  &-sticky.is-stuck + &-shadow
    c 0 0 $alt
  &-meta
    c $text $border
.js-navigation-open
  ease color
table.files
  c $text $border $hl
  tr
    &.navigation-focus, &[aria-selected=true]
      td
        c 0 0 $hl
  td
    c 0 $border
    &.age
      c $text
    &.icon
      c $text
      .octicon-file-directory
        c $blue
.merge-pr-more-commits,
.status-meta,
.TimelineItem
  &:before
    c 0 0 $border
  &-badge, &--condensed &-badge
    c $hover 0 $bg
  &-body
    c $faded
.border,
.rgh-collaborator
  b $b
.btn
  bgi none
  c $cn $border $hl
  ease background-color, border-color
  &.btn-sm
    c 0 0 $alt
  &:disabled
    &, &:hover
      c $faded $border $hl
  &:hover
    bgi none
    c $cn $blue $region
.auth-form-body
  c $text $border $alt
.topic-tag
  b $b
  &-outline
    s inset 0 0 0 1px $border
  &-link:hover
    c 0 0 $region
  c $text $border $alt
  ease background-color
  &-action
    &, button, a
      c $blue $blue $blueTrans
.access-token
  bbc $border
div.listgroup-item
  c $text $border
.reponav-item
  b none
  &.selected
    bc $blue $border transparent
  &:focus, &:hover
    c $blue
  c $blue
  .octicon
    c $indigo
    filter contrast(0.5)
    o 0.8
  &.selected
    bc $hl $border transparent
    c $blue 0 $bg
    .octicon
      c $text
      filter none
      o 1
.pagehead-tabs-item,
.pagehead,
.repohead.experiment-repo-nav
  c 0 $border $alt
  // @stlint-ignore
  @media (min-width: 768px)
    c $alt
.repohead,
.repo-list
  .Label--outline
    c $text $border
.site-header
  c 0 $border $alt
.showcase-page-pattern::after
  c 0 $alt $border
.write-content.js-write-bucket
  s none
.form-
  &group &control
    c $cn $border $alt
    .search-input
      caret-color $blue
    &:focus
      c 0 0 $hl
  &control, &select
    c $cn $border $alt
.flash
  c $cn $border $bg
  &-warn
    c $yellow $border $alt
.menu
  c $text $border $alt
  &, .blankslate
    c $alt $border
  &-heading
    c $cn $border $alt
  &-item
    bb $b
    ease background-color
    &:hover
      c 0 0 $region
    &.selected
      c $blue 0 $hl
.boxed-group
  &-inner, & .heading, & > h3
    c 0 $border $alt
.rule,
hr
  bb $b
.file
  c 0 $border
  &-header
    c $blue $border $alt
    m 0
.pl-s > a
  c $green
:not(.commit-build-statuses) > a:not(.IssueLabel):not(.IssueLabel--big):not(.lh-condensed-ultra):not(.module-linker):not(.tabnav-tab)
  c $text
  &:hover
    c $blue
div.commit-build-statuses,
.link-gray
  c $faded
  &:hover
    c $blue
  &-dark, body &-dark
    c $blue
    &:hover, body &:hover
      td underline
.access-form-wrapper
  c 0 $border $alt
.discussion-sidebar-heading
  c $text
.table-list-header-toggle .btn-link.selected
  &:hover
    c $blue
.footer,
.site-footer
  bt $b
.file-wrap
  c 0 $border
.table-list
  bb $b
  &-header
    c 0 $border $alt
    &-toggle .btn-link:hover
      c $blue
  &-cell
    bt $b
  &-bordered &-cell
    &:first-child
      bl $b
    &:last-child
      br $b
.breadcrumb
  c $blue
  &-item-selected, &-item[aria-current]
    c $blue
  strong.final-path
    c $cn
.drag-and-drop
  c $blue $border $alt
.octofication .message,
.overall-summary
  c 0 $border $alt
.btn-link:not(.IssueLabel):not(.tabnav-tab), .subnav-search-context .btn
  c $blue
.btn.btn-primary, .js-selected-navigation-item.subnav-item
  c 0 0 $hl
.social-count
  c $blue 0 $hl
  &:hover
    c $cn
.subnav-item
  c 0 $border
  ease border-color, background-color
  &:focus, &:hover
    c 0 0 $icon
  &.selected
    &, &:focus, &:hover
      c 0 $border $hl
.input-contrast
  box()
.btn-octicon
  c $blue
  &:hover
    c $cn
  &-danger:hover
    c $red
.more-repos
  c 0 0 $alt
  s inset 0 1px 0 $border
.news
  .alert
    bt $b
  .Details
    &-content--hidden li, button
      btc $border
    button:hover
      bg transparent
      td underline
.Counter
  c $blue 0 $border
*:not(code) .Box
  c 0 $border $alt
.tag
  &-info
    c $blue
    ../-name
      c $text
.note, .help
  c $blue
.site-subnav.bg-wite
  c 0 0 $alt
  s-2()
.application-main
  .flex-wrap
    c 0 0 transparent
  & > main
    & > div.border-bottom.border-gray-light
      c 0 0 $bg
    & > .site-subnav + div
      c 0 0 $bg
      .text-gray-light
        c $text
.contrib-person path
  v $orange
.menu-item
  &.selected, &[aria-current]
    &:before
      c 0 0 $blue
.CheckRun
  c 0 $border $alt
  &-header
    c 0 $border
.checks-list-item
  &.selected &-name
    c $cn 0 $region
.diffbar
  c 0 0 $bg
.progress-bar
  c 0 0 $hl
.pinned-item-
  &list-item &handle
    c $faded
.user-status-container-border-busy
  b $b
  c 0 $yellow $yellowTrans
.js-user-status-container
  c 0 0 $hl
.protip & >
  code
    c $text $border $hl
  strong
    c $yellow
    td underline
  svg
    v $yellow
.Popover-message
  c $text $border
  &--top-right
    caret "bottom" $alt $alt
  &--top-left
    caret "bottom" $hl
  &--bottom-left
    caret "top"
.UnderlineNav-item
  &:hover
    bbc $region
  &.selected, &[aria-current], &[role="tab"][aria-selected="true"]
    bbc $blue
.gh-header-number
  c $orange
  fw 400
  td underline
.toolbar-item
  &, & .menu-target
    &:hover
      c $blue $hl
    c $text
    ease color
.body > div.d-flex
  px 10px
.pagination-loader-container
  bgi none
  bt $b
  pt 16px
  .Box
    ease border-color
    & > *
      ease background-color
      ../:hover, ../:focus
        c 0 $blue
        & > *
          c 0 0 $hl
  &, .bg-white
    c 0 0 $alt
.ajax-pagination-form .ajax-pagination-btn
  c $blue
.blankslate
  c $text $border $hl
  code
    c $text $border $bg
.content.p-4.text-center.rounded-1.border.border-gray-light
  c 0 0 $alt
.Box
  &--responsive, &row--unread
    s inset 2px 0 0 $blue
  &-header
    c 0 $border $alt
    &--blue
      c $blue $border $alt
  &-row
    &:first-of-type
      bt 0
    bt $b
    &--focus-gray.navigation-focus
      c 0 0 $icon
    &[style*="background"]
      c 0 0 $region
  &&--condensed &-row
    c 0 $border $alt
  &-row--
    &focus-gray
      c 0 0 $hl
    &yellow
      c $text $yellow transparent
    ../--responsive &unread::before
      bgi none
      c 0 0 $blue
.text
  c $faded
.border-
  &top
    bt $b
  &left
    bl $b
  &right
    br $b
  &bottom
    bb $b
.repository-content > div.bg-white:first-child
  c 0 0 $bg
.SideNav
  c $text $border $alt
  &-item
    c 0 $border
    ease background-color, color
    &[aria-current='page'], &[aria-selected='true']
      c 0 0 $hl
      &::before
        c 0 0 $blue
    &:focus, &:hover
      c 0 0 $hl
      &::before
        c 0 0 $icon
    &:last-child
      s 0 1px 0 $border
section[style*="#fafbfc"]
  c 0 0 $bg
.reponame-suggestion
  c $green
#tree-finder-field
  c 0 0 $bg
.boxed-group-inner
  c $text
.tree-browser-result[aria-selected="true"]
  c 0 0 $hl
