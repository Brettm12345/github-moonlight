.graph-filter .info
  c $text
line[stroke="#000"]
  v 0 $border
text
  &[fill="#000"], &[fill="#6a737d"], &[text-anchor="middle"]
    v $text
.viz
  .code-frequency
    ../, &
      c $text $border $alt
    p 8px
.c
  &add
    c $green
  &del
    c $red
.conversation-list-heading
  c $faded $border
  .inner
    b $b
    c $faded
    p 0 10px
    r 2px
.num, relative-time
  c $faded
.simple-conversation-list
  c $faded
  & > li
    c 0 $border
.graphs
  .dir
    c $text
  .area
    v $teal
.code-frequency
  .addition
    v $green
  .deletion
    v $red
.community-checklist .progress
  c 0 0 $region
table.capped-list
  td
    c $text $border $alt
  tr:nth-child(2n), th
    c $text $border $alt
.summary-stats li
  &, & > .num, & > a
    ease background-color
  a
    ease background-color, color
  &:hover, &:focus
    &, & > .num, & > a
      c $cn $border $hl
  /.traffic-graph-stats, &, & > .num
    c $text $border $alt
.pulse-section
  c $faded
