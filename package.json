{
  "name": "github-moonlight",
  "version": "1.0.0",
  "main": "index.js",
  "author": "brettm12345",
  "license": "MIT",
  "scripts": {
    "build": "stylus -U ./github.user.styl",
    "build:watch": "yarn build --watch",
    "lint": "stlint src",
    "lint:fix": "yarn lint --fix",
    "lint:watch": "yarn lint --watch"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn build"
    }
  },
  "dependencies": {
    "stylus": "^0.54.7"
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "stlint": "^1.0.62",
    "stlint-v4fire": "^1.0.37",
    "stylint": "^2.0.0",
    "stylus-supremacy": "^2.14.0"
  }
}
