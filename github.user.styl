@require './src/easings'

$base1 = var(--base-1)
$base2 = var(--base-2)
$base3 = var(--base-3)
$base4 = var(--base-4)
$base5 = var(--base-5)
$base6 = var(--base-6)
$base7 = var(--base-7)
$base8 = var(--base-8)
$base9 = var(--base-9)
$borderAlt = var(--border-alt)
$active = var(--active)
$b = 1px solid $base1
$blue = var(--blue)
$blueDark = var(--blue-dark)
$blueTrans = var(--blue-trans)
$cn = var(--cn)
$cyan = var(--cyan)
$cyanLight = var(--cyan-light)
$cyanTrans = var(--cyan-trans)
$faded = var(--faded)
$green = var(--green)
$indigo = var(--indigo)
$monoFont = var(--mono-font)
$orange = var(--orange)
$pink = var(--pink)
$placeholder = var(--placeholder)
$purple = var(--purple)
$purpleTrans = var(--purple-trans)
$red = var(--red)
$redLight = var(--red-light)
$redTrans = var(--red-trans)
$sb = var(--sb)
$sbWidth = var(--sb-width)
$wbSbSize = var(--wb-sb-size)
$wbSbRadius = var(--wb-sb-radius)
$teal = var(--teal)
$tealTrans = var(--teal-trans)
$text = var(--text)
$textAlt = var(--text-alt)
$textParagraph = var(--text-paragraph)
$textDark = var(--text-dark)
$textGray = var(--text-gray)
$uiFont = var(--ui-font)
$yellow = var(--yellow)
$yellowTrans = var(--yellow-trans)

@require './src/mixins'

@-moz-document domain('github.com'), domain('raw.githubusercontent.com')
  @require './src'
@-moz-document domain('gist.github.com')
  @require './src/gist'
@-moz-document domain('developer.github.com')
  @require './src/developer'
